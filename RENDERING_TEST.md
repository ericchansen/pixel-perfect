# Rendering Test Page — emoji=2.5

GitHub renders emoji at exactly **2.5 monospace columns**.

**Rules:**
- ✅ Even emoji count per line → **perfect** alignment (integer total)
- ⚠️ Odd emoji count per line → **impossible** to align (±0.5 col error)
- ✅ No emoji → **perfect** alignment

All boxes/tables below were generated by the `aifmt` fixer (not hand-written).

---

## Test 1: No-emoji box (control — expect PERFECT)

```
┌───────────────────┐
│ Packages: 42      │
│ Tests: 100%       │
└───────────────────┘
```

## Test 2: 2-emoji box (even count — expect PERFECT)

```
┌──────────────────────┐
│ 📦 ✅ Status: OK    │
│ 🚀 ❌ Deploy: Fail  │
│ Plain text here      │
└──────────────────────┘
```

## Test 3: 1-emoji box (odd count — fixer WARNS, ±0.5 unavoidable)

```
┌───────────────────┐
│ 📦 Packages: 42  │
│ ✅ Tests: 100%   │
│ 🚀 Deployed: Yes │
│ Plain text here   │
└───────────────────┘
```

> ⚠ The fixer warns: "alignment off by 0.5 cols (1 emoji — odd count)"
> On GitHub, these lines will be ~0.5 cols too narrow on the right.

## Test 4: No-emoji table (control — expect PERFECT)

| Status | Package | Count |
| ------ | ------- | ----- |
| Pass   | numpy   | 42    |
| Fail   | pandas  | 3     |

## Test 5: 2-emoji table (even count — expect PERFECT)

| Status     | Package |
| ---------- | ------- |
| ✅📦 Pass | numpy   |
| ❌🚀 Fail | pandas  |
| ok         | torch   |

## Test 6: 1-emoji table (odd count — fixer WARNS, ±0.5 unavoidable)

| Status   | Package | Count |
| -------- | ------- | ----- |
| ✅ Pass | numpy   | 42    |
| ❌ Fail | pandas  | 3     |
| ⚠️ Warn | scipy   | 7     |
| ok       | torch   | 100   |

> ⚠ The fixer warns: "cell has 1 emoji (odd count → fractional width)"
> Status column pipes will be ~0.5 cols off.

## Test 7: Width reference (raw diagnostic)

```
XXXXX|      <- 5 chars
📦📦|       <- 2 emoji = 5.0 cols (should align with 5 chars)

XXXXXXX|    <- 7 chars
XXXXXXXX|   <- 8 chars
📦📦📦|     <- 3 emoji = 7.5 cols (should fall between 7 and 8)
```

**Check:** Does `📦📦|` align exactly with `XXXXX|`?
